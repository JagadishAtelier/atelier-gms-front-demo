
  # Gym Management System App (Community)

  This is a code bundle of Gym Management System App by Atelier.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  